---
phase: 03-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - README.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-04

must_haves:
  truths:
    - "README.md exists at repo root with Chinese+English bilingual section headings"
    - "README covers Windows/PowerShell local dev setup for both backend and frontend"
    - "README covers Docker build and start steps including how to copy .env.example"
    - "README covers Vercel frontend deployment including Root Directory setting and NEXT_PUBLIC_API_URL env var"
    - "README documents all four env vars (ZHIPU_API_KEY, GLM_MODEL, ALLOWED_ORIGINS, NEXT_PUBLIC_API_URL) in a table"
    - "README includes the mixed-content (HTTPS to HTTP) warning and PYTHONUNBUFFERED note"
    - "Human has confirmed NEXT_PUBLIC_API_URL is set in Vercel dashboard and frontend deploys successfully"
  artifacts:
    - path: "README.md"
      provides: "Complete deployment walkthrough"
      contains: "docker compose up -d"
    - path: "README.md"
      provides: "Env var reference table"
      contains: "NEXT_PUBLIC_API_URL"
  key_links:
    - from: "README.md Quick Start Docker section"
      to: "cyber-god/backend/.env.example"
      via: "cp .env.example .env instruction"
      pattern: "cp .env.example .env"
    - from: "README.md Vercel section"
      to: "NEXT_PUBLIC_API_URL"
      via: "Vercel Environment Variables dashboard step"
      pattern: "NEXT_PUBLIC_API_URL"
---

<objective>
Write the repo-root README.md as the complete deployment guide, then pause for human to verify Vercel frontend deployment.

Purpose: DEPLOY-04 requires Windows/PowerShell local dev, Docker build+push, and Vercel deploy in one linear walkthrough. DEPLOY-01 requires confirming the Vercel deployment works with NEXT_PUBLIC_API_URL set.
Output: README.md + human-verified Vercel frontend deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deployment/03-RESEARCH.md
@.planning/phases/03-deployment/03-UI-SPEC.md
@.planning/phases/03-deployment/03-01-SUMMARY.md
</context>

<interfaces>
<!-- README structure contract from 03-UI-SPEC.md -->
<!--
Required heading hierarchy:
  # 赛博财神爷 (Cyber God of Wealth)
  ## 功能特性 (Features)
  ## 快速开始 (Quick Start)
    ### 本地开发 (Local Development)
      #### 后端 (Backend)
      #### 前端 (Frontend)
    ### Docker 部署 (Docker Deployment)
    ### Vercel 前端部署 (Vercel Frontend Deployment)
  ## 环境变量 (Environment Variables)
  ## 架构说明 (Architecture)
  ## 注意事项 (Known Issues / Gotchas)

Formatting rules (from UI-SPEC):
  - Every heading: Chinese + English in parentheses
  - All code blocks: language specified (```bash, ```dockerfile, ```yaml, ```typescript)
  - Shell commands: PowerShell syntax; bash alternatives as inline comments
  - No inline HTML — pure Markdown only
  - No emojis in headings or code blocks
  - File paths in code blocks: forward slashes even on Windows
  - Placeholder API key: sk-your-zhipu-api-key-here (never real values)
  - docker compose (v2, no hyphen) — never docker-compose
  - pnpm only (no npm/yarn alternatives)
  - Python venv activation: .\.venv\Scripts\Activate.ps1
-->

<!-- Copywriting contract from 03-UI-SPEC.md -->
<!--
Title:     赛博财神爷 (Cyber God of Wealth)
Subtitle:  AI 财务顾问 · 毒舌财神 · GLM-5 驱动
ZHIPU_API_KEY description:   智谱 AI API 密钥 — 从 open.bigmodel.cn 获取
GLM_MODEL description:       模型名称，默认 glm-4-flash（免费快速），推荐生产用 glm-4-5
ALLOWED_ORIGINS description: 逗号分隔的 CORS 来源列表，例如 https://your-app.vercel.app,http://localhost:3000
NEXT_PUBLIC_API_URL description: 后端 URL，构建时注入，更改后需重新部署
Step 1 copy note:    cp .env.example .env  then  # 填入 ZHIPU_API_KEY
Docker start:        docker compose up -d
Docker stop:         docker compose down
PYTHONUNBUFFERED note: PYTHONUNBUFFERED=1 is set in the Dockerfile — required to prevent MCP subprocess stdio hang inside Docker
Mixed-content warning: Vercel (HTTPS) requires the backend to also serve HTTPS. For local demo, use http://localhost:8000. For public demo, add TLS (e.g., Caddy or Cloudflare Tunnel).
Port conflict note:  Stop any local uvicorn dev server before running docker compose up — both use port 8000
-->

<!-- Frontend env var wiring confirmed in cyber-god/frontend/app/page.tsx -->
<!--
api: `${process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:8000'}/api/chat`
The fallback to localhost:8000 means local dev works without the env var set.
-->

<!-- Vercel monorepo Root Directory setting from RESEARCH.md -->
<!--
Vercel project settings > General > Root Directory: cyber-god/frontend
Vercel project settings > Environment Variables > Production:
  NEXT_PUBLIC_API_URL = https://your-docker-host:8000
-->
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write README.md at repo root</name>
  <files>
    README.md
  </files>
  <read_first>
    - .planning/phases/03-deployment/03-UI-SPEC.md (mandatory structure, formatting rules, copywriting contract — follow exactly)
    - .planning/phases/03-deployment/03-RESEARCH.md (PowerShell commands, Docker pitfalls, env var names, open questions about HTTPS + public IP)
    - cyber-god/backend/.env.example (confirm env var names and placeholder values as written in Plan 01)
    - cyber-god/frontend/.env.local.example (check if it documents NEXT_PUBLIC_API_URL)
    - cyber-god/backend/main.py (confirm entry point is main:app for Docker CMD reference)
  </read_first>
  <action>
Create README.md at the repo root. The file must contain exactly the six sections below, in order, following all UI-SPEC formatting rules (bilingual headings, language-tagged code blocks, no inline HTML, no docker-compose hyphenated, pnpm only, no real API keys).

SECTION 1 — Title and subtitle:

  # 赛博财神爷 (Cyber God of Wealth)

  AI 财务顾问 · 毒舌财神 · GLM-5 驱动

SECTION 2 — ## 功能特性 (Features)

  5 bullet points:
  - GLM-5 powered snarky verdict streamed token by token in Chinese
  - Tool-call transparency: shows price lookup and savings impact calculation status in real time
  - Savings progress bar animates on agent response; flashes red when a purchase dents your savings
  - localStorage persistence for savings goal (存款目标) and current savings (已存金额)
  - One-command Docker backend (`docker compose up -d`) + Vercel-hosted frontend

SECTION 3 — ## 快速开始 (Quick Start)

  ### 本地开发 (Local Development)

  #### 后端 (Backend)

  PowerShell code block (language: powershell):
    cd cyber-god/backend
    python -m venv .venv
    .\.venv\Scripts\Activate.ps1
    pip install -r requirements.txt
    cp .env.example .env
    # Edit .env: fill in ZHIPU_API_KEY
    uvicorn main:app --reload --port 8000

  Note after the block (plain text): "Bash users: replace activation with `source .venv/bin/activate`. `cp` is the same."

  #### 前端 (Frontend)

  PowerShell code block (language: powershell):
    cd cyber-god/frontend
    pnpm install
    # Create cyber-god/frontend/.env.local:
    # NEXT_PUBLIC_API_URL=http://localhost:8000
    pnpm dev

  Note after the block: "The frontend defaults to http://localhost:8000 if NEXT_PUBLIC_API_URL is not set."

  ### Docker 部署 (Docker Deployment)

  Numbered list of 4 steps:
  1. Copy .env.example to .env and fill in ZHIPU_API_KEY. Set ALLOWED_ORIGINS to include your Vercel URL for production.
  2. Build and start: show bash code block with `cd cyber-god/backend` then `docker compose up -d`
  3. Verify: `curl http://localhost:8000/docs` — expected: FastAPI Swagger UI HTML
  4. Stop: `docker compose down`

  After the steps, two callout notes as blockquotes:
  - "> Stop any local uvicorn dev server before running docker compose up — both use port 8000."
  - "> The browser calls the backend directly. Ensure port 8000 is accessible from the internet, or use a tunnel (e.g., `ngrok http 8000`) for a quick public demo."

  ### Vercel 前端部署 (Vercel Frontend Deployment)

  Numbered list of 4 steps:
  1. Connect your GitHub repo to Vercel (vercel.com > New Project > Import).
  2. In Project Settings > General > Root Directory: set to `cyber-god/frontend`.
  3. In Project Settings > Environment Variables (Production): add `NEXT_PUBLIC_API_URL` = `https://your-docker-host-or-domain:8000`.
  4. Deploy: push to main or click "Redeploy" in the Vercel dashboard.

  After the steps, two callout notes as blockquotes:
  - "> NEXT_PUBLIC_API_URL is baked into the JS bundle at build time. If the backend URL changes, trigger a new deployment."
  - "> Vercel (HTTPS) requires the backend to also serve HTTPS. For local demo, use http://localhost:8000. For a public demo, add TLS (e.g., Caddy or Cloudflare Tunnel)."

SECTION 4 — ## 环境变量 (Environment Variables)

  Markdown table with header row: Variable | Required | Default | Description

  Four data rows (use the exact descriptions from the copywriting contract):
  - ZHIPU_API_KEY | Yes | — | 智谱 AI API 密钥 — 从 open.bigmodel.cn 获取
  - GLM_MODEL | No | glm-4-flash | 模型名称，默认 glm-4-flash（免费快速），推荐生产用 glm-4-5
  - ALLOWED_ORIGINS | No | http://localhost:3000 | 逗号分隔的 CORS 来源列表，例如 https://your-app.vercel.app,http://localhost:3000
  - NEXT_PUBLIC_API_URL | No (frontend) | http://localhost:8000 | 后端 URL，构建时注入，更改后需重新部署

SECTION 5 — ## 架构说明 (Architecture)

  2–4 sentence paragraph (no diagrams, no headings below this level):
  "User sends a purchase impulse via the chat UI. The FastAPI backend spawns a local MCP price server as a subprocess, calls it to look up mock prices, then runs a savings impact calculation. It streams a GLM-5-powered snarky verdict in Vercel Data Stream Protocol format (f:, 0:, 2:, e:, d: lines). The frontend progress bar animates when the 2: data chunk arrives."

SECTION 6 — ## 注意事项 (Known Issues / Gotchas)

  Numbered list of 5 items:
  1. `PYTHONUNBUFFERED=1` is set in the Dockerfile — required to prevent MCP subprocess stdio hang inside Docker.
  2. Stop any local uvicorn dev server before running `docker compose up` — both use port 8000.
  3. `NEXT_PUBLIC_API_URL` is baked into the JS bundle at build time. Changing it in the Vercel dashboard requires a new deployment.
  4. Vercel (HTTPS) to HTTP backend is blocked by browsers (mixed-content policy). For a public demo, add TLS to the Docker host (e.g., Caddy reverse proxy or Cloudflare Tunnel).
  5. `ALLOWED_ORIGINS` must include the exact Vercel deployment URL with no trailing slash. Using `*` disables CORS protection.
  </action>
  <verify>
    - `grep "赛博财神爷 (Cyber God of Wealth)" README.md` returns a match
    - `grep "docker compose up -d" README.md` returns a match (Docker v2, no hyphen)
    - `grep "docker-compose" README.md` returns NO match (no v1 hyphenated form)
    - `grep "NEXT_PUBLIC_API_URL" README.md` returns a match
    - `grep "PYTHONUNBUFFERED" README.md` returns a match
    - `grep "Root Directory" README.md` returns a match
    - `grep "pnpm" README.md` returns a match
    - `grep "npm install\|yarn install" README.md` returns NO match
    - `grep "29eaf2f\|sk-29" README.md` returns NO match (no real API key)
  </verify>
  <done>
    README.md exists at repo root with all six sections, bilingual headings, PowerShell local dev commands, Docker startup steps, Vercel deployment steps, env var table with all four variables using UI-SPEC copywriting, and the five gotcha items. No real secrets. No inline HTML. No hyphenated docker-compose.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Docker build and Vercel deployment end-to-end</name>
  <files></files>
  <action>No automated action — this checkpoint collects human verification of the Docker build and Vercel deployment.</action>
  <verify>Human confirms Docker builds successfully and Vercel frontend chats with backend without CORS errors.</verify>
  <done>Human types "approved" after Docker and Vercel verification passes.</done>
  <what-built>
    Plan 03-01 created the Docker packaging files (Dockerfile, docker-compose.yml, .dockerignore, .env.example) and fixed the MCP subprocess command (python3). Plan 03-02 Task 1 wrote the README.md. The backend is ready to containerize and the frontend is deployable to Vercel.
  </what-built>
  <how-to-verify>
    Perform these verification steps in order:

    Step 1 — Docker build and start (run from repo root in PowerShell):

    ```powershell
    cd cyber-god/backend
    # If .env does not exist yet: cp .env.example .env then edit it
    docker compose build
    docker compose up -d
    # Wait ~10 seconds for lifespan startup
    curl http://localhost:8000/docs
    # Expected: HTML response (FastAPI Swagger UI page)
    docker compose down
    ```

    If the build fails, paste the error output in your reply.

    Step 2 — Vercel frontend deployment:
    1. Go to vercel.com, create a New Project, import this GitHub repo.
    2. In Project Settings > General > Root Directory: enter `cyber-god/frontend` and save.
    3. In Project Settings > Environment Variables (Production): add `NEXT_PUBLIC_API_URL` = the public URL of your Docker host (e.g., `http://your-server-ip:8000` or an ngrok URL like `https://abc123.ngrok.io`).
    4. Trigger a deployment (push a commit to main or use "Redeploy" in Vercel dashboard).
    5. Visit the deployed Vercel URL.
    6. Type "我想花 800 买个盲盒" in the chat input and submit.
    7. Confirm the verdict streams token by token and the progress bar updates.

    Step 3 — CORS check:
    Open browser DevTools (F12) > Console. Look for any `CORS` or `Access-Control-Allow-Origin` error messages during the chat request. There should be none.
  </how-to-verify>
  <resume-signal>
    Type "approved" if Docker builds successfully and the Vercel frontend chats with the backend without errors.
    Or describe any issues (Docker error log, CORS console errors, network errors).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README instructions to user machine | README guides user to run docker commands and set Vercel env vars; instructions must not expose real secrets |
| Vercel build environment to NEXT_PUBLIC_API_URL | Build-time env var is inlined into public JS bundle; the backend URL is readable by anyone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | README.md code examples | mitigate | All env var values in README use placeholder format (sk-your-zhipu-api-key-here); verified by grep check in task verify step |
| T-03-07 | Information Disclosure | NEXT_PUBLIC_API_URL in JS bundle | accept | This is an intentionally public backend URL; inlining it in the JS bundle is the standard Next.js pattern and does not expose secrets |
| T-03-08 | Spoofing | Vercel CORS without exact origin | mitigate | README instructs user to set exact Vercel URL in ALLOWED_ORIGINS; wildcard risk noted in 注意事项 section item 5 |
| T-03-09 | Denial of Service (UX) | Mixed-content HTTPS to HTTP browser block | mitigate | README includes explicit warning about TLS requirement for Vercel (HTTPS) to Docker host (HTTP); ngrok suggested as quick demo alternative |
</threat_model>

<verification>
After both tasks complete (README written + checkpoint approved):

```bash
# README exists at repo root
ls README.md

# Required sections present
grep "快速开始 (Quick Start)" README.md
grep "环境变量 (Environment Variables)" README.md
grep "注意事项" README.md

# No v1 docker-compose (hyphenated) in README
grep "docker-compose" README.md || echo "PASS: no v1 docker-compose"

# NEXT_PUBLIC_API_URL documented
grep "NEXT_PUBLIC_API_URL" README.md

# No real API key leaked
grep "29eaf2f" README.md || echo "PASS: no real key"
```
</verification>

<success_criteria>
- README.md at repo root with all six required sections
- Bilingual headings (Chinese + English in parentheses) throughout
- PowerShell commands for both backend and frontend local dev
- Docker steps include copy .env.example, docker compose up -d, and docker compose down
- Vercel steps include Root Directory setting and NEXT_PUBLIC_API_URL env var
- Env var table documents all four variables with descriptions matching UI-SPEC copywriting contract
- Five gotcha items present in 注意事项 section
- Human approves: Docker builds and Vercel frontend chatting with backend works (no CORS errors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-deployment/03-02-SUMMARY.md`
</output>
